## Synopsis
1. create a new atlas alert using the Atlas API in the currently created testing database cluster define in config.json
2. the alert would send an email if there is a change in storage or tier on the default cluster
3. this new script would be call create_alert.py
4. the script would use already developed logic from cluster-admin.py